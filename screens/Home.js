import React from 'react';
import {useState} from 'react';
import { View, Text, FlatListItem, SafeArea, SafeAreaView } from 'react-native';
import { COLORS, NFTData} from '../constants'

const Home = () => {
  return (
    <SafeAreaView style={{flex: 1}}>
       
    </SafeAreaView>
  );
}

export default Home;
